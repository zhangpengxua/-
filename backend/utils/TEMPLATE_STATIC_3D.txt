import numpy as np
import matplotlib.pyplot as plt
from mpl_toolkits.mplot3d import Axes3D

def generate_static_geometry_view():
    fig = plt.figure(figsize=(10, 8))
    ax = fig.add_subplot(111, projection='3d')
    
    # [Slot 1: 自动填装文档二中的函数表达式或边界格点数据]
    # {{INJECT_MESHGRID_RANGE_AND_RESOLUTION}}
    x_range = np.linspace(-2, 2, 200)
    y_range = np.linspace(-2, 2, 200)
    X, Y = np.meshgrid(x_range, y_range)
    
    # 执行参数化注入
    Z = {{INJECT_FUNCTION_FROM_POOL_OR_USER}}
    
    # 渲染基础空间曲面
    surf = ax.plot_surface(X, Y, Z, cmap='coolwarm', alpha=0.6, edgecolor='none')
    
    # [Slot 2: 自动填装文档三中的特定点位或高亮切平面]
    if {{HAS_POINT_TRIGGER}}:
        px, py, pz = {{INJECT_POINT_COORDINATES}}
        ax.scatter([px], [py], [pz], color='crimson', s=150, zorder=5, label='Target Anchor Point')
        # 联动注入局部切平面或特征光栅线代码
        # {{INJECT_TANGENT_PLANE_GENERATION_CODE}}
        
    ax.set_xlabel('X-Axis')
    ax.set_ylabel('Y-Axis')
    ax.set_zlabel('Z-Axis')
    ax.view_init(elev=30, azim=-45) # 设定标准全局透视视角
    plt.title("Static Parametric Infrastructure View")
    plt.show()